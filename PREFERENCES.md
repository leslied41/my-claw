# Preferences

- Always ask for approval before using web search. Never use web search without explicit permission in that conversation.
  - **Exception**: Job hunting skills (`/job-hunt`, `/job-review`, `/job-apply`, `/job-status`) and their associated cron jobs are pre-approved to use Brave Search and WebFetch autonomously. No per-session approval needed.
